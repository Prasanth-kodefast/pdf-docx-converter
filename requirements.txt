fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
pdf2docx==0.5.8
fpdf2==2.8.2
requests==2.32.3
# Track-Changes DOCX exporter (editorjs_to_docx.py) — python-docx builds
# the base document while lxml inserts the `w:ins` / `w:del` OOXML that
# python-docx doesn't wrap natively. BeautifulSoup walks the EditorJS
# inline HTML (mentions / clause chips / suggestion marks) without hand-
# rolling a parser.
python-docx==1.1.2
lxml==5.2.2
beautifulsoup4==4.12.3
